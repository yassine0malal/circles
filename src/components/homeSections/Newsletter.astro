<section class="py-24 px-8 max-w-4xl mx-auto text-center">
  <p class="text-xs font-light tracking-[0.3em] uppercase text-ink-soft/70 mb-6">Stay connected</p>
  <h2 class="font-display text-4xl md:text-5xl font-medium text-ink leading-tight mb-6">
    Subscribe to our<br /><span class="italic">newsletter</span>
  </h2>
  <p class="text-base font-light text-ink-soft/70 mb-12 max-w-md mx-auto">
    Receive reflections, circle openings, and insights for a healthier mind.
  </p>

  <form id="newsletter-form" class="flex flex-col sm:flex-row gap-4 max-w-lg mx-auto">
    <input 
      type="email" 
      placeholder="your@email.com" 
      class="flex-1 bg-transparent border border-line rounded-full px-8 py-4 text-sm font-light text-ink placeholder:text-ink-soft/50 focus:outline-none focus:border-line transition-colors duration-300"
    />
    <button type="submit" class="px-10 py-4 bg-ink text-cream text-sm font-light tracking-wide rounded-full hover:bg-sage transition-colors duration-500">
      Subscribe
    </button>
  </form>
  <p class="text-xs font-light text-ink-soft/50 mt-6">By subscribing you agree to our privacy policy</p>
</section>

<script>
  const form = document.getElementById("newsletter-form");
  if (form) {
    form.addEventListener("submit", (e) => {
      e.preventDefault();
      // TODO: wire up your newsletter API here
    });
  }
</script>